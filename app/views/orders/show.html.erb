<p class="flash" style="color: green"><%= notice %></p>

<%= render @order %>

<div>
  <%= link_to "Edit", edit_order_path(@order) %> |
  <%= link_to "Back", orders_path %>

  <%= button_to "Destroy", @order, method: :delete %>
</div>